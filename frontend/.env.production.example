# Point the frontend at your live backend (Render URL). Set this as an
# Environment Variable in the Vercel project settings (Production + Preview).
NEXT_PUBLIC_API_URL=https://wc2026-api.onrender.com
